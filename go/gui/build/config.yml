version: "3"

info:
  companyName: "Grant Howard"
  productName: "Touhou Tagger"
  productIdentifier: "io.github.the1812.touhou-tagger"
  description: "Touhou Project music metadata tagger"
  copyright: "Copyright Grant Howard"
  comments: "Touhou Tagger desktop application"
  version: "2.3.0"

dev_mode:
  root_path: .
  log_level: warn
  debounce: 1000
  ignore:
    dir:
      - .git
      - .task
      - frontend
      - ../bin
    file:
      - "*_test.go"
    watched_extension:
      - "*.go"
    git_ignore: true
  executes:
    - cmd: wails3 build DEV=true
      type: blocking
    - cmd: wails3 task dev:frontend
      type: background
    - cmd: wails3 task run
      type: primary
